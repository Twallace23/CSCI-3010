#include "catch.hpp"
#include <iostream>
#include <vector>

int main(){
    // Sums all numbers in a vector and returns the resulting value
    std::vector<int> numbers {1,2,3,4,5};
    //Sum(std::vector<int> numbers);
    //print(Sum(std::vector<int> numbers);
}
/*ff
    // takes an int, n, and returns the nth value of the fibonacci sequence (1, 1, 2, 3, 5, 8, 13, ...)
int NthFibonacci(int n){
	// 1 + 1 = 2 + 1 = 3 + 2 = 5 + 3 = 8
	// For each number in the sequence we need to add the previous index to the current index
	if(n == 0){
		return 0;
	}
	if(n == 1){
		return 1;
	}
	return NthFibonacci(n-1) + NthFibonacci(n-2);
}
// returns -1 if the number is negative and 1 if positive
int Sign(int num);
	return num > 0 ? -1: 1;
*/